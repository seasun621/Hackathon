# 실제 도시형 절차적 생성 시스템 개편안

## 1. 문서 목적

현재 도시 생성 시스템은 건물 아키타입과 도로 위계를 갖추고 있지만, 도시의 기본 단위가 `128m 청크 → 십자형 도로 → 사분면별 건물 1동`으로 고정되어 있다. 이 구조는 멀리서 도시의 실루엣을 만드는 데는 유리하지만, 가까이에서 보면 건물이 독립된 장난감처럼 놓이고 도로와 블록의 반복 패턴이 쉽게 드러난다.

이 문서는 다음 목표를 달성하기 위한 도시 생성 시스템 개편안을 정의한다.

- 실제 도시처럼 도로, 블록, 필지, 건물이 서로 영향을 주며 생성되도록 한다.
- 지역마다 건물뿐 아니라 블록 구성과 개발 방식이 다르게 보이도록 한다.
- 공용 지오메트리와 `InstancedMesh`를 유지하여 성능 저하를 억제한다.
- 로프 이동과 스윙의 재미를 도시 계획 단계부터 보장한다.
- 청크 경계에서도 도로와 건물이 자연스럽게 이어지도록 한다.

핵심 생성 순서는 다음과 같다.

```text
도시 골격 → 도로 그래프 → 블록 → 필지 → 가로벽 → 건물군 → 디테일 → 이동성 검증
```

---

## 2. 현재 구조의 핵심 문제

### 2.1 청크가 도시 계획 단위와 렌더링 단위를 동시에 담당한다

현재는 각 청크 중앙을 지나는 수직·수평 도로와 네 개의 부지가 반복된다. 따라서 다음 현상이 발생한다.

- 모든 교차로와 블록 크기가 비슷하다.
- 도시 전체에서 건물 간격이 반복된다.
- 대각선 도로와 강이 기존 격자 위에 덧붙여진 장식처럼 보인다.
- 청크마다 최대 네 동의 큰 건물이 중앙에 놓여 연속된 도시 가로벽이 형성되지 않는다.
- 플레이어가 이동하면서 생성 규칙을 빠르게 알아차릴 수 있다.

### 2.2 건물 종류는 다양하지만 도시 조직은 동일하다

아키타입이 달라도 같은 크기의 부지 중앙에 비슷한 방식으로 배치된다. 건물 형태의 차이보다 배치 규칙의 반복이 더 강하게 보이기 때문에 결과적으로 도시 전체가 하나의 세트처럼 느껴진다.

### 2.3 높이가 도시 활동보다 랜드마크 거리와 무작위 값에 좌우된다

실제 도시의 고층 군집은 대로 교차점, 역세권, 상업 중심, 수변 재개발, 용도 규제와 개발 시기의 결과다. 단일 랜드마크를 중심으로 한 동심원형 높이 구배는 읽기 쉽지만 인공적인 패턴을 만든다.

### 2.4 건물 저층부와 블록 내부가 부족하다

도시의 현실감은 스카이라인뿐 아니라 다음 요소에서 강하게 발생한다.

- 도로를 따라 이어지는 건물 전면
- 상점, 로비, 캐노피와 출입구
- 골목, 중정, 후면 증축부
- 보도, 연석, 횡단보도와 주차 차량
- 옥상 설비, 물탱크, 난간과 방화벽

이 요소가 부족하면 고층 건물이 많아도 축소 모형처럼 보인다.

---

## 3. 새로운 생성 원칙

### 3.1 청크는 스트리밍과 렌더링에만 사용한다

`128m` 청크는 다음 역할만 담당한다.

- 현재 플레이어 주변 콘텐츠 로딩 및 해제
- 해당 영역과 겹치는 도로·건물 인스턴스 렌더링
- 물리 충돌체와 로프 앵커 등록 및 해제

도로, 블록, 필지와 건물 계획은 청크보다 큰 매크로 셀에서 먼저 결정한다. 하나의 도로와 블록은 여러 청크를 가로지를 수 있어야 한다.

권장 매크로 셀 크기:

- 기본: `8×8` 청크
- 넓은 중심지 또는 수변: `12×12`~`16×16` 청크
- 같은 월드 시드와 매크로 셀 좌표는 항상 같은 결과를 생성해야 한다.

### 3.2 도시를 건물이 아니라 도로 그래프에서 시작한다

도로는 독립된 청크 텍스처가 아니라 서로 연결된 그래프다.

각 도로는 다음 정보를 가진다.

```ts
interface RoadSegment {
  id: string;
  roadClass: 'local' | 'collector' | 'avenue' | 'arterial';
  start: Vec2;
  end: Vec2;
  width: number;
  lanes: number;
  sidewalkWidth: number;
  curvature: number;
  bridge: boolean;
  elevated: boolean;
  districtTags: DistrictKind[];
}
```

도로망은 완전한 격자 대신 변형된 격자를 사용한다.

- 약 65%: 직교 격자
- 약 15%: 좁고 짧은 구도심 도로
- 약 10%: 장거리 대각선 간선도로
- 약 5%: 공원·광장·역·강으로 끊기거나 우회하는 도로
- 약 5%: 고가도로, 램프, 교량 접근로

권장 도로 간격:

| 도로 종류 | 권장 폭 | 권장 간격/길이 |
|---|---:|---:|
| 생활가로 | 10~16m | 45~110m 간격 |
| 집산도로 | 18~26m | 120~260m 간격 |
| 일반 대로 | 28~36m | 280~500m 간격 |
| 광역 대로 | 42~56m | 700~1,200m 간격 |
| 대각선 도시축 | 24~46m | 1~3km 연속 |

도로 폭과 간격에는 지역별 오차를 주되, 한 도로가 이어지는 동안 등급과 폭이 이유 없이 급변하지 않도록 한다.

---

## 4. 블록 생성

도로 그래프가 완성되면 도로 사이의 닫힌 영역을 찾아 도시 블록으로 변환한다. 블록은 청크 경계와 독립적이다.

### 4.1 지역별 블록 크기

| 지역 | 권장 블록 크기 |
|---|---|
| 상업 중심 | 55~90m × 70~120m |
| 구도심 | 35~65m × 50~90m |
| 일반 주거 | 70~140m × 90~180m |
| 고층 주거 단지 | 100~180m × 120~220m |
| 산업·수변 | 100~220m |
| 시민·문화 지구 | 건축물과 광장을 포함한 비정형 대형 블록 |

대각선 도로, 강, 철도, 공원 경계와 만나는 블록은 삼각형·사다리꼴·곡선형 등 비정형으로 유지한다. 이를 직사각형으로 강제 보정하지 않는다.

### 4.2 블록 데이터

```ts
interface CityBlock {
  id: string;
  polygon: Vec2[];
  district: DistrictKind;
  developmentEra: DevelopmentEra;
  landValue: number;
  transitAccess: number;
  waterfrontAccess: number;
  preservation: number;
  frontageRoads: string[];
  parcels: ParcelPlan[];
}
```

블록 단위로 지역, 개발 연대, 재료 팔레트와 평균 높이를 공유한다. 개별 건물이 완전히 독립적으로 결정되지 않게 하는 것이 중요하다.

---

## 5. 필지 분할과 가로벽

### 5.1 블록 전면을 작은 필지로 분할한다

도로와 접하는 블록 전면을 다음 범위의 필지로 나눈다.

- 구도심·근린 상업: 전면 폭 6~16m
- 일반 주거·중층 상업: 12~28m
- 오피스·복합 개발: 여러 필지를 합쳐 30~70m
- 대형 공공·산업 시설: 50m 이상

예시:

```text
80m 블록 전면
[12m 상가][8m 상가][18m 아파트][10m 상가][32m 오피스 포디움]
```

같은 블록의 반대편 전면은 다른 분할을 사용한다. 필지 합병 확률은 지가, 개발 연대와 지역에 따라 결정한다.

### 5.2 건물을 도로 경계에 맞춘다

- 구도심·중층 건물: 도로 경계에서 0~2m 후퇴
- 일반 주거: 2~6m 후퇴
- 상업 타워 포디움: 0~4m 후퇴
- 타워 상부: 포디움 가장자리에서 6~15m 후퇴
- 시민 건축: 의도적인 전면 광장과 12~35m 후퇴 허용

중층 건물은 도로를 따라 연속된 가로벽을 형성해야 한다. 모든 건물을 필지 중앙에 놓지 않는다.

### 5.3 블록 내부 구성

각 블록 내부에는 지역에 따라 다음 중 하나 이상을 배치한다.

- 중정
- 서비스 골목
- 소규모 주차장
- 낮은 후면 별동
- 건물 증축부
- 단지 내부 정원
- 하역 공간
- 연결 통로

블록 안쪽이 모두 빈 공간이 되거나 하나의 거대한 건물로 채워지는 현상을 피한다.

---

## 6. 지역별 형태 문법

건물 아키타입의 선택 확률만 바꾸지 않고 블록과 필지의 생성 방식 자체를 지역마다 다르게 한다.

### 6.1 구도심

- 좁고 불규칙한 필지
- 4~10층 중심
- 벽돌과 석재 외벽
- 도로에 붙은 연속 가로벽
- 옥상 물탱크, 굴뚝, 방화벽
- 일부 필지만 재개발된 신축 중고층

### 6.2 상업 코어

- 높은 필지 합병률
- 3~8층 포디움
- 포디움 위에서 후퇴한 20~55층 타워
- 대로 교차점과 역세권에 높이 집중
- 로비, 캐노피와 넓은 코너 출입구
- 블록 내부 서비스 골목과 설비 공간

### 6.3 고층 주거 지구

- 넓은 블록
- 블록당 2~5개의 슬래브 또는 타워
- 단지 내부 녹지와 보행로
- 도로변에는 낮은 상업 포디움
- 비슷한 재료와 창문 규격을 공유하는 건물군

### 6.4 시민·문화 지구

- 넓은 전면 광장
- 계단, 기념물, 축선과 대칭 배치
- 낮고 육중한 공공 건물과 간헐적 타워
- 주변 일반 블록보다 낮은 밀도
- 공원과 대로가 시각적으로 연결됨

### 6.5 수변 지구

- 강변 산책로와 연속 녹지
- 건물 전면이 수변 또는 접근로를 향함
- 강 바로 앞은 낮은 포디움 위주
- 두 번째 열부터 간헐적인 고층 타워
- 교량 진입부 주변 광장과 상업 시설
- 수변을 완전히 차단하는 연속 고층 벽 금지

### 6.6 산업 전환 지구

- 창고, 공장, 주차장과 신축 타워 혼합
- 넓은 필지와 불규칙한 건축선
- 오래된 산업 시설 일부 보존
- 대형 로프트 건물과 신규 주거 포디움 공존

### 6.7 근린 상업·주거 지구

- 5~12층 중심
- 1층 상점이 이어지는 주요 가로
- 뒷골목 쪽으로 낮아지는 높이
- 작은 공원, 학교와 생활 시설
- 고층 건물은 대로 교차점에만 제한적으로 배치

---

## 7. 건물 높이와 스카이라인

### 7.1 높이 산정 방식

높이를 단순 범위 추첨으로 정하지 않고 다음 요소를 조합한다.

```text
건물 높이 =
지역 기본 층수
× 용도 계수
× 접한 도로 폭 계수
× 지가 계수
× 교통 접근성 계수
× 개발 연대 계수
× 제한된 변동값
```

권장 범위:

| 조건 | 일반적인 층수 |
|---|---:|
| 좁은 생활가로 | 4~9층 |
| 일반 대로변 | 8~18층 |
| 광역 대로변 | 15~35층 |
| 상업 코어 | 20~55층 |
| 주요 역세권 | 일부 45~75층 |
| 특별 랜드마크 | 60층 이상 |

### 7.2 높이 군집 규칙

- 같은 블록의 건물은 기준 높이를 공유한다.
- 건물별 높이 변화보다 블록 사이의 높이 변화가 더 커야 한다.
- 고층은 대로 교차점, 역세권, 상업 중심과 재개발 블록에 군집한다.
- 보존 지구와 구도심은 중심지와 가까워도 낮게 유지될 수 있다.
- 랜드마크로부터 멀어질수록 균일하게 낮아지는 동심원 구배는 사용하지 않는다.
- 고층 군집 사이에 중층 안부와 저층 보존 지역을 두어 스카이라인에 리듬을 만든다.

---

## 8. 개발 연대와 재개발 흔적

블록마다 `developmentEra`를 부여한다.

| 시기 | 주요 특징 |
|---|---|
| 1900~1930 | 벽돌, 좁은 필지, 장식 코니스, 옥상 물탱크 |
| 1930~1960 | 석재, 아르데코, 계단형 셋백 |
| 1960~1990 | 콘크리트, 슬래브, 넓은 전면 플라자 |
| 1990~2010 | 유리 포디움 타워, 대형 오피스 블록 |
| 2010 이후 | 대규모 복합 개발, 비정형 크라운, 수변 재개발 |

연대는 단순 외형 선택값이 아니라 다음 요소에 영향을 준다.

- 평균 필지 폭
- 필지 합병률
- 건축선과 후퇴 거리
- 건물 높이와 포디움 여부
- 재료와 창문 리듬
- 옥상 설비 종류
- 블록 내부 공간 구성

재개발 블록에서는 오래된 저층 건물 여러 동 사이에 새 타워 한 동을 배치할 수 있다. 모든 건물이 같은 시기에 지어진 것처럼 보이지 않게 한다.

---

## 9. 건물 조립 시스템

개별 건물 모델을 대량 생성하지 않고 공용 부품을 조합한다.

### 9.1 공용 인스턴스 종류

- 기본 매스: 박스, 원통, 쐐기
- 포디움과 타워 매스
- 셋백 단
- 크라운과 첨탑
- 창문 패널과 벽돌 패널
- 코니스와 수직 리브
- 캐노피, 어닝과 간판
- 옥상 난간, HVAC, 물탱크와 안테나
- 가로수, 가로등과 차량

### 9.2 현실감을 위한 저층부 규칙

- 상업 지역의 1층 층고를 상부층보다 크게 설정한다.
- 8~20m 간격으로 출입구 또는 상점 전면을 배치한다.
- 코너 필지는 양쪽 도로를 향한 전면을 가진다.
- 캐노피와 어닝의 돌출 깊이를 0.8~2.5m로 제한한다.
- 건물 사이에는 0.5~3m 폭의 틈이나 방화벽을 선택적으로 만든다.
- 파사드 평면에 0.2~1m 깊이 변화를 준다.
- 창문 텍스처의 밝기뿐 아니라 창 간격, 층고와 수직 리듬을 팔레트별로 다르게 한다.

### 9.3 성능 원칙

- 건물 하나마다 별도 `Mesh`나 `Material`을 만들지 않는다.
- 매크로 셀 또는 렌더 청크 단위로 인스턴스 데이터를 묶는다.
- 재료 팔레트 수를 제한하고 인스턴스 색상으로 변형한다.
- 충돌체는 외형 전체가 아니라 저층 매스와 상부 매스 1~2개로 근사한다.
- 원거리 청크에서는 저층부 디테일과 옥상 소품을 생략한다.

---

## 10. 스윙과 로프 이동성 규칙

도시 생성 후 `TraversalValidator`가 건물과 앵커를 그래프로 변환해 이동 가능성을 검사한다.

### 10.1 권장 이동 조건

- 마주 보는 주요 앵커 간격: 35~80m
- 일반 스윙 구간의 건물 높이: 최소 18~25m
- 스윙 캐니언 권장 폭: 20~55m
- 플레이어 주변 115m 안에 다음 후보 앵커가 최소 2개 존재
- 하나의 앵커에서 다음 앵커로 이어지는 유효 경로가 막히지 않아야 함
- 3~5개 이상의 저층 블록이 연속될 경우 보정 경로 제공

### 10.2 앵커 배치

앵커는 단순히 건물 높이 비율로 추가하지 않고 이동 그래프의 연결성을 기준으로 배치한다.

- 지붕 모서리와 타워 상부
- 포디움과 타워 접합부
- 건물 중간 셋백 테라스
- 대로변 코너 면
- 교량 구조물과 고가도로 하부
- 대형 표지 구조물과 튼튼한 가로 시설물

높은 초고층에는 기존처럼 중간 앵커를 제공하되, 실제로 주변 건물과 연결되는 면에 우선 배치한다.

### 10.3 이동성 보정 순서

이동 그래프가 끊긴 경우 다음 순서로 보정한다.

1. 기존 건물의 적절한 면에 중간 앵커 추가
2. 코너 건물 높이 또는 셋백 높이를 소폭 조정
3. 가로등·표지판·교량 구조물 등 보조 앵커 추가
4. 저층 블록에 중층 코너 건물 삽입
5. 최후 수단으로 필지의 건물 아키타입 교체

도시 전체를 이동성 때문에 고층으로 채우지 않는다.

---

## 11. 특수 공간 생성 규칙

### 11.1 랜드마크

- `10×10` 청크마다 고정적으로 배치하지 않는다.
- 상업 코어, 광역 대로 교차점, 역세권과 수변 축 등 도시적으로 의미 있는 후보지에서 선택한다.
- 랜드마크 사이 최소 거리를 두되 완전히 동일한 간격으로 반복하지 않는다.
- 모든 랜드마크 주변 세 필지를 비우지 않는다.
- 랜드마크 유형에 따라 광장, 포디움, 역사 건물군 또는 공원과 결합한다.

### 11.2 공원과 광장

- 남은 빈 필지를 단순 공원으로 바꾸지 않는다.
- 소공원, 근린공원, 선형공원, 중앙광장과 건물 전면광장을 구분한다.
- 공원 가장자리에는 연속된 건물 전면을 배치해 공간의 경계를 만든다.
- 광장에는 진입 축, 포장 패턴과 주변 1층 상업 시설을 제공한다.

### 11.3 강과 수변

- 강 중심선은 월드 좌표 기반으로 연속 생성한다.
- 강과 겹치는 건물을 사후 제거하는 대신 강을 도로망보다 먼저 지형 제약으로 제공한다.
- 도로 그래프는 교량 후보 지점에서만 강을 횡단한다.
- 교량 접근로는 인접 도로와 부드럽게 연결한다.
- 수변 산책로와 첫 번째 건물 열 사이에 최소한의 공개 공간을 확보한다.

### 11.4 시작 지점

- 시작 지점은 회전교차로 하나만 놓기보다 도시의 첫 인상을 보여 주는 조정된 구역으로 만든다.
- 2~3개의 명확한 이동 방향을 제공한다.
- 중층 가로벽, 고층 목표물과 열린 광장을 동시에 볼 수 있게 한다.
- 시작 후 수 초 내에 사용할 수 있는 낮은 앵커와 중간 높이 앵커를 제공한다.

---

## 12. 권장 코드 구조

```text
src/game/city/
├─ CityGenerator.ts          전체 생성 흐름과 캐시 관리
├─ MacroPlan.ts              매크로 셀 단위 결정론적 계획
├─ RoadGraphGenerator.ts     장거리 도로망 생성
├─ BlockExtractor.ts         도로 사이 블록 폴리곤 추출
├─ ParcelSubdivider.ts       블록 전면과 필지 분할
├─ DistrictField.ts          지역·지가·교통·연식 결정
├─ BlockGrammar.ts           지역별 건물군 생성 규칙
├─ BuildingAssembler.ts      공용 인스턴스 조립
├─ TraversalValidator.ts     로프 이동 그래프 검사와 보정
├─ CityChunkRenderer.ts      청크별 인스턴스·충돌체 표시
└─ types.ts                  공용 계획 데이터 타입
```

역할 구분:

```text
MacroPlan
  └─ 도로, 지역, 블록과 건물 계획을 월드 좌표로 생성

CityChunkRenderer
  └─ 현재 청크와 겹치는 계획 데이터를 읽어 화면과 물리에 등록

TraversalValidator
  └─ 생성 결과를 검사하고 최소한의 이동성 보정 수행
```

현재 `UrbanPlan.ts`의 `createChunkUrbanPlan()`은 매크로 계획 조회 함수로 대체한다. `City.ts`의 `createChunk()`는 도시 계획을 직접 결정하지 않고 현재 청크와 교차하는 계획 객체를 렌더링하는 역할만 맡는다.

---

## 13. 단계별 구현 계획

### 1단계: 고정 4필지 구조 제거

- 청크의 네 사분면에 건물 한 동을 놓는 루프 제거
- 블록과 필지 계획 타입 추가
- 월드 시드 기반 결정론적 ID와 해시 함수 정리

완료 기준:

- 같은 청크 안에 크기가 다른 여러 건물이 배치된다.
- 청크 경계를 가로지르는 블록과 건물이 중복 생성되지 않는다.

### 2단계: 매크로 도로 그래프와 가변 블록 도입

- 장거리 도로 그래프 생성
- 도로 사이의 블록 폴리곤 추출
- 대각선 도로와 강을 처음부터 그래프 제약으로 반영

완료 기준:

- 도로가 청크 경계에서 끊기거나 폭이 갑자기 바뀌지 않는다.
- 서로 다른 크기와 형태의 블록이 나타난다.

### 3단계: 필지 분할과 연속 중층 가로벽 생성

- 블록 전면 분할
- 지역별 필지 합병률 적용
- 건축선과 후퇴 규칙 적용
- 코너 필지 처리

완료 기준:

- 도로를 따라 4~12층 건물이 연속적으로 보인다.
- 모든 건물이 필지 중앙에 고립되어 보이지 않는다.

### 4단계: 포디움과 타워 규칙 적용

- 포디움 높이와 전면 점유율 결정
- 타워 후퇴와 방향 결정
- 대로 교차점과 역세권 중심 높이 군집 적용

완료 기준:

- 상업 코어가 단순한 고층 랜덤 집합이 아니라 연속된 저층부와 타워군으로 보인다.

### 5단계: 지역별 연식과 재료 팔레트 적용

- `developmentEra` 추가
- 필지 크기, 높이, 재료와 옥상 소품에 연대 반영
- 일부 블록에 재개발 혼합 규칙 적용

완료 기준:

- 인접 블록은 서로 관련되어 보이면서도 개발 시기에 따른 차이가 읽힌다.

### 6단계: 이동 그래프 검사

- 로프 앵커를 노드로 변환
- 사거리, 방향, 높이 차와 장애물을 고려한 연결 생성
- 고립 구간 자동 감지 및 보정

완료 기준:

- 주요 이동 경로에서 115m 사거리 안에 다음 앵커가 최소 두 개 존재한다.
- 넓은 대로와 저층 지역에서도 이동 흐름이 끊기지 않는다.

### 7단계: 저층부·옥상·도로 디테일 추가

- 상점, 로비, 출입구와 캐노피
- 옥상 설비, 물탱크와 난간
- 보도, 연석, 횡단보도, 가로수와 주차 차량
- 원거리 LOD 또는 생성 생략 규칙

완료 기준:

- 지상과 중간 고도에서 건물 용도와 지역 차이가 읽힌다.
- 기존 목표 프레임 성능을 유지한다.

### 8단계: 추가 아키타입 확장

기본 도시 조직이 안정된 뒤 필요한 건물 형태를 추가한다. 아키타입 수보다 블록 문법과 저층부 변형을 먼저 완성한다.

---

## 14. 생성 품질 검증 지표

### 시각적 지표

- 500m 이동 구간에서 동일한 블록 패턴이 그대로 반복되지 않는다.
- 한 화면에 최소 세 종류의 블록 크기 또는 형태가 보인다.
- 중층 가로벽, 포디움 타워와 열린 공간이 함께 나타난다.
- 같은 지역의 건물은 재료와 층고가 어느 정도 통일된다.
- 서로 다른 지역 경계에서는 블록 구조와 가로 분위기가 분명히 달라진다.

### 이동성 지표

- 주요 플레이 공간의 앵커 그래프 연결률 98% 이상
- 플레이어 위치 115m 이내 유효 앵커 후보 중앙값 3개 이상
- 이동 불가능한 연속 저층 구간 1블록 이하
- 넓은 대로 횡단에 사용할 수 있는 양방향 경로 존재
- 시작 지점에서 즉시 선택 가능한 이동 방향 2개 이상

### 성능 지표

- 건물별 개별 재료 생성 금지
- 청크당 드로 콜과 인스턴스 수 예산 설정
- 충돌체는 단순 매스 중심으로 제한
- 매크로 계획 캐시 재사용
- 동일 월드 시드에서 생성 결과와 ID가 항상 일치

---

## 15. 최종 원칙

실제 도시처럼 보이게 만드는 핵심은 건물 아키타입의 개수가 아니다. 하나의 블록이 같은 도로, 역사, 지가와 개발 논리의 영향을 받아 형성된 것처럼 보여야 한다.

다음 원칙을 개발 판단의 우선순위로 사용한다.

1. 개별 건물보다 도로와 블록을 먼저 만든다.
2. 청크 경계보다 도시 구조의 연속성을 우선한다.
3. 무작위성보다 지역별 일관성을 우선한다.
4. 고층 스카이라인보다 연속된 저층 가로벽을 먼저 완성한다.
5. 현실적인 형태와 스윙 가능성을 별도 검증 단계로 함께 보장한다.
6. 공용 인스턴스를 유지하되 배치, 비례, 재료와 디테일의 조합으로 다양성을 만든다.
7. 새 아키타입은 기존 블록 문법으로 표현할 수 없는 필요가 있을 때만 추가한다.

---

## 16. 로그라이크 생존 게임용 공간 구조

도시는 현실적인 외관을 유지하되, 전투·성장·보상 루프가 읽히는 공간 구조를 함께 제공한다. 이 규칙은 맵 생성 메타데이터만 정의하며 전투, 경제와 아이템 시스템에 직접 의존하지 않는다.

### 공간 역할

| 공간 | 도시 형태 | 향후 사용 목적 |
|---|---|---|
| 안전 허브 | 시작 회전교차로와 넓은 공개 공간 | 상점, 아이템 선택, 스테이지 준비 |
| 일반 시가지 | 좁은 가로와 중층 가로벽 | 이동 전투, 자원 수집, 일반 적 웨이브 |
| 전투 광장 | 두 개 이상의 열린 필지와 주변 건물벽 | 정예 웨이브, 생존 이벤트, 보스 전 단계 |
| 보상 랜드마크 | 고층 랜드마크와 주변 광장 | 상자, 보상 선택, 다음 목표 유도 |
| 수변·교량 | 긴 이동축과 제한된 횡단 지점 | 추격전, 위험 구간, 특수 이벤트 |

### 위험 권역

- 시작점은 위험도 0으로 고정한다.
- 시작점으로부터 약 5청크마다 위험도를 한 단계 높인다.
- 위험도는 최대 5단계로 제한한다.
- 위험도는 향후 적 체력이나 보상을 직접 계산하지 않고 맵 쪽 메타데이터로만 제공한다.
- 전투 시스템은 이 값을 읽어 웨이브 테이블, 보상 배율과 이벤트 종류를 선택할 수 있다.

### 전투 공간 원칙

- 전투 광장은 완전히 빈 평면이 아니라 열린 필지와 건물벽을 함께 가진다.
- 광장 주변에는 로프 앵커가 있는 중·고층 건물을 남긴다.
- 적에게 포위되더라도 빠져나갈 수 있는 도로 방향을 최소 두 개 제공한다.
- 차량, 가로수와 낮은 시설물은 시야를 완전히 차단하지 않는 범위에서 장애물로 사용한다.
- 다음 전투 공간은 대로, 대각선 축 또는 보이는 랜드마크를 통해 자연스럽게 유도한다.

### 현재 구현 메타데이터

```ts
type GameplayZone = 'safe-hub' | 'combat-arena' | 'reward-landmark' | 'city';

interface GameplayZonePlan {
  kind: GameplayZone;
  dangerTier: number;
  arenaVariant: number;
}
```

이 데이터는 `MacroCityPlan.ts`에서 결정론적으로 생성되며 `UrbanPlan.ts`를 통해 렌더 청크에 전달된다. 추후 스테이지 디렉터가 추가되더라도 도시 생성기가 게임 시스템을 직접 호출하지 않도록 단방향 데이터 연결을 유지한다.
