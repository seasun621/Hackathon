# SUPER SWING 프로토타입 개발 계획

## 1. 목표

외부 모델링 없이 `TypeScript + Vite + Three.js + Rapier 3D`만으로 다음 한 문장을 검증한다.

> 로프 이동과 직접 조준 사격을 동시에 수행할 때, 빠르고 위험하게 날수록 더 높은 점수를 얻는 플레이가 재미있는가?

완성 기준은 시작 화면부터 90초 런, 지면·옥상을 포함한 연속 이동, 결과와 최고 기록까지 한 번의 플레이 흐름이 끊김 없이 동작하는 것이다.

## 2. 첫 수직 슬라이스 범위

### 이동

- 1인칭 마우스 시점과 포인터 고정
- 화면 중앙 근처 건물 앵커 자동 선택 및 시각적 표시
- 왼쪽 버튼을 누르면 부착, 누르는 동안 로프 길이 단축, 놓으면 해제
- Rapier 동적 플레이어와 중력, 건물 충돌, 로프 장력
- WASD 공중 보정, 지면·옥상 저속 보행, Space 포커스 슬로모션
- 지면과 옥상은 정상 플레이 공간이며 물리 월드 밖으로 이탈했을 때만 안전 복귀

### 사격과 표적

- 오른쪽 버튼 히트스캔 사격
- 일반 표적: 기본 점수
- 황금 표적: 높은 점수와 콤보 보너스
- 폭탄 위협: 플레이어에게 접근하며, 추적 박스 잠금 타이밍에 파괴하면 보너스
- 폭탄이 플레이어에게 도달하면 감점과 콤보 초기화
- 명중, 오발, 폭탄에 서로 다른 시청각 피드백

### 점수와 한 판

- 점수 = 표적 기본값 × 콤보 × 속도 × 거리 × 중앙 명중
- 90초 실시간 제한
- 최고 속도, 최고 콤보, 명중률 기록
- 브라우저 로컬 저장소 최고 점수
- 결과 화면과 즉시 재시작

### 표현

- 절차 생성/제거되는 다양한 높이와 외형의 로우폴리 도시 블록
- 늦은 오후 하늘, 따뜻한 햇빛과 대기 안개, 발광 표적, 로프, 속도선
- 차선이 있는 연결 도로, 보도와 정지 차량
- 코드로 조립한 왼손 로프 장치와 오른손 총
- 왼손 장치에서 앵커까지 빠르게 전개되는 교체 가능한 로프 발사 연출
- 총구 섬광, 피격 파편, 가벼운 화면 흔들림, 합성 효과음

## 3. 구현 순서

1. Vite/TypeScript 프로젝트와 렌더 루프를 만든다.
2. Rapier 플레이어, 카메라, 입력, 재시작/일시정지를 연결한다.
3. 도시 청크와 건물 충돌체, 앵커 후보를 절차 생성한다.
4. 로프 선택 보정, 장력, 길이 단축, 로프 표시를 구현한다.
5. 3종 표적과 히트스캔, 점수 계산을 구현한다.
6. HUD, 런 상태, 최고 기록, 피드백을 추가한다.
7. 빌드와 실제 브라우저 조작을 검증하고 수치를 조정한다.

## 4. 기술 원칙

- 렌더 프레임과 물리 스텝을 분리하고 물리는 고정 간격으로 처리한다.
- 제한시간은 슬로모션과 무관한 실제 시간으로 계산한다.
- 로프 앵커는 정확한 픽셀 조준보다 화면 위치, 거리, 높이로 보정한다.
- 생성한 청크가 플레이어에게서 멀어지면 메시와 Rapier 바디를 함께 제거한다.
- 가로·세로 도로는 교차부까지 하나의 지오메트리로 만들어 같은 높이의 면이 겹치지 않게 한다.
- 도로 폭은 골목·일반·대로의 세 등급만 공유하고 같은 도로선에서는 청크가 바뀌어도 폭을 유지한다.
- 대각선 도로는 정해진 구역 규칙으로만 생성해 경로 다양성은 확보하되 임의 메시 폭증을 막는다.
- 건물, 상부 층, 옥상 장식, 창문, 차량은 청크 단위 `InstancedMesh`와 공유 머티리얼을 사용한다.
- 첫 청크 묶음 이후에는 프레임당 새 청크 하나만 만들고, 멀어진 청크도 프레임당 하나씩 제거한다.
- 첫 버전에는 탄약, 재장전, 탄환 비행시간, 복잡한 파괴 물리를 넣지 않는다.

## 5. 플레이테스트 기준

- 10초 안에 로프를 걸고 전진할 수 있는가?
- 로프가 걸린 상태에서도 자유롭게 표적을 조준할 수 있는가?
- 표적 3종을 색과 형태만으로 즉시 구분할 수 있는가?
- 고속 명중이 저속 명중보다 점수상 명확히 유리한가?
- 지면이나 옥상에 착지한 뒤 로프를 다시 걸어 흐름으로 복귀할 수 있는가?
- 직교 도로망에서 막힌 구역 없이 직진과 90도 회전이 가능한가?
- 골목, 일반 도로, 대로, 대각선 구역이 플레이 중 형태와 폭으로 구분되는가?
- 이동 중 도로 교차부나 대각선 연결부가 깜빡이지 않는가?
- 90초 동안 도시 생성/제거로 눈에 띄는 끊김이 없는가?

## 6. 후속 후보

첫 플레이테스트에서 핵심 조작이 재미있다는 판단이 난 뒤에만 시계, 연료통, 방패, 드론, 로프 절단기, 속도 링, 보스 추격전을 순서대로 검토한다.
